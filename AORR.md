# AORR 상태 머신 설계

이 문서는 프로페셔널 개인 웹사이트와 `Games` 탭의 곱셈 게임을 GitHub Pages 정적 사이트로 구현하기 위한 실행 가능한 AORR 상태 머신이다.

현재 저장소 기준:
- 루트 파일은 거의 비어 있음
- `index.html`, `styles.css`, `script.js`는 아직 없음
- 외부 백엔드 없이 HTML, CSS, JavaScript만으로 동작해야 함
- `Step 1` 기준 검색에서 `[게임 추가 기능:]` 항목은 확인되지 않음

## 1. Target

| 항목 | 정의 |
|---|---|
| 프로페셔널 웹사이트 개발 목표 | 아이들을 위한 게임 모음을 제공하는 정적 개인 웹사이트를 만든다. 자기소개와 이력은 게임 중심 소개 콘텐츠로 대체한다. |
| GitHub Pages 배포 목표 | 저장소 루트에서 바로 서빙 가능한 정적 사이트로 완성한다. 별도 서버 없이 GitHub Pages에서 동작해야 한다. |
| 입력 자료 | 개인 이름, 소개 문구, 경력/프로젝트 정보 [사람 확인 필요], 사이트 톤, Games 탭 요구사항, 게임 난이도/조작 방식, 로고나 이미지 사용 여부 [사람 확인 필요] |
| 필수 페이지와 섹션 | Home, Games, 소개/프로필 섹션, 게임 설명 섹션, 연락처 또는 외부 링크 섹션 [사람 확인 필요], 푸터 |
| Games 탭 요구사항 | 상단 내비게이션에 `Games` 탭을 제공한다. 탭은 게임 섹션으로 이동하거나 별도 정적 섹션을 연다. |
| 곱셈 게임 요구사항 | `Games` 탭에 어린아이용 곱셈 게임을 포함한다. 화면 탭 시작, 클릭형 키패드 입력, 모바일 터치 조작을 모두 지원한다. 랜덤 문제, 자리수 난이도, 점수, 재시작 흐름이 있어야 한다. |
| 데스크톱 완료 기준 | 1280px 전후 화면에서 레이아웃이 깨지지 않고, 탭 이동과 게임 조작이 명확해야 한다. |
| 모바일 완료 기준 | 360px 폭 기준으로 가로 스크롤이 없어야 하고, 버튼과 텍스트가 겹치지 않아야 한다. 터치만으로 게임이 진행되어야 한다. |

### Target 상태 머신

| 상태 | 의미 |
|---|---|
| `READY` | 입력 자료와 범위가 정리되어 개발 시작 가능 |
| `ACTING` | 현재 루프의 구현 또는 설계 변경을 수행 중 |
| `VERIFYING` | 파일, UI, 동작, 페이지 호환성을 검사 중 |
| `RETRYING` | 같은 루프에서 실패 원인을 하나만 수정하는 중 |
| `PASSED` | 해당 루프의 목적과 검증 기준을 충족 |
| `DEPLOY_READY` | 모든 개발 검증이 끝났고 배포만 남음 |
| `DEPLOYING` | GitHub Pages 설정 또는 배포를 진행 중 |
| `DEPLOYED` | GitHub Pages에 반영 완료 |
| `BLOCKED` | 외부 권한, 환경, 충돌 때문에 진행 불가 |
| `HITL_REQUIRED` | 사람의 콘텐츠 확인 또는 의사결정이 필요 |

## 2. Act

| 항목 | 정의 |
|---|---|
| 한 번의 개발 루프에서 수행할 최소 작업 | 하나의 실패 원인 또는 하나의 독립 기능만 수정한다. 예: 레이아웃만, 게임 입력만, 점수만. |
| 수정 가능한 파일 범위 | 해당 루프와 직접 관련된 최소 파일만 수정한다. 예: `index.html`, `styles.css`, `script.js`, 게임 전용 JS 파일. |
| 생성할 수 있는 파일 | 루트의 `index.html`, `styles.css`, `script.js`, 필요 시 게임 보조 JS 파일. GitHub Pages 호환 정적 자산만 허용한다. |
| 실행 가능한 로컬 검증 명령어 | `git status --short`, `rg --files`, 브라우저에서 정적 파일 열기, 필요 시 로컬 정적 서버 실행 [사람 확인 필요], 콘솔 오류 확인, 모바일/데스크톱 시뮬레이션 확인 |

### Act 규칙

1. 한 번에 하나의 목표만 바꾼다.
2. 기존에 통과한 기능을 건드리지 않는다.
3. 정적 사이트 원칙을 유지한다.
4. 게임 로직과 UI는 분리 가능한 범위로 나눈다.
5. 코드 수정 전에 입력 자료가 부족하면 `HITL_REQUIRED`로 전환한다.

## 3. Observe

| 항목 | 관찰 대상 |
|---|---|
| 파일 생성 여부 | 요구 파일이 실제로 존재하는지 확인한다. |
| HTML, CSS, JavaScript 오류 | 문법 오류, 누락된 참조, 렌더 실패를 확인한다. |
| 로컬 웹서버 응답 | 정적 파일이 정상 응답하는지 확인한다. [사람 확인 필요] |
| 브라우저 콘솔 오류 | 실행 중 예외, 리소스 로드 실패, 경고를 확인한다. |
| 데스크톱 및 모바일 화면 | 레이아웃, 오버플로, 탭 동작, 가독성을 확인한다. |
| 키보드 및 터치 게임 조작 | 게임이 조작 방식별로 동일하게 동작하는지 확인한다. |
| GitHub Pages 호환성 | 상대 경로, 정적 자원, 빌드 의존성 없음, 서버 전용 기능 없음 여부를 확인한다. |

### Observe 결과 해석

- 파일이 없으면 `HTML_STRUCTURE` 또는 `ENVIRONMENT`
- 화면이 깨지면 `CSS_RESPONSIVE`
- 게임 입력이 안 되면 `GAME_CONTROL`
- 문제 생성, 점수, 진행이 틀리면 `GAME_LOGIC`
- 데이터나 소개 문구가 비어 있으면 `CONTENT`
- 검증 수단이 없으면 `TEST`
- 배포가 막히면 `GITHUB_PERMISSION` 또는 `DEPLOYMENT`

## 4. Reason

실패 원인은 아래 중 하나로만 분류한다.

| 분류 | 기준 |
|---|---|
| `HTML_STRUCTURE` | 태그 구조, 누락된 섹션, 잘못된 문서 뼈대 |
| `CSS_RESPONSIVE` | 모바일/데스크톱 레이아웃, 오버플로, 반응형 붕괴 |
| `JAVASCRIPT` | 스크립트 문법, 런타임 예외, 상태 관리 오류 |
| `GAME_LOGIC` | 점수, 충돌, 랜덤 생성, 승패/진행 규칙 오류 |
| `GAME_CONTROL` | 키보드, 터치, 클릭, 포커스, 입력 흐름 오류 |
| `CONTENT` | 소개 문구, 이력, 프로젝트 설명, 카피 부족 또는 충돌 |
| `TEST` | 검증 수단 부족, 테스트 절차 누락, 재현 불가 |
| `ENVIRONMENT` | 로컬 실행 환경, 파일 경로, 브라우저, 권한 이슈 |
| `GITHUB_PERMISSION` | 토큰, 저장소 권한, Pages 설정 접근 문제 |
| `DEPLOYMENT` | Pages 배포 실패, 루트 경로, 공개 반영 지연 |
| `UNKNOWN` | 위 분류로 확정 불가 |

## 5. Repeat

| 규칙 | 설명 |
|---|---|
| 한 번에 하나의 실패 원인만 수정 | 서로 다른 결함을 묶지 않는다. |
| 관련된 최소 파일만 변경 | 수정 범위를 줄여 회귀를 최소화한다. |
| 수정 후 동일한 Verifier 재실행 | 같은 검증 절차를 다시 수행한다. |
| 기존에 통과한 기능에 대한 회귀 테스트 실행 | 기존 통과 항목이 깨지지 않았는지 확인한다. |

### Repeat 상태 전이

`VERIFYING`에서 실패하면 `RETRYING`으로 간다.  
`RETRYING`에서 수정 후 다시 `VERIFYING`으로 돌아간다.  
같은 fingerprint가 2회 반복되면 `BLOCKED` 또는 `HITL_REQUIRED`로 전환한다.

## 6. Stop

아래 조건 중 하나면 해당 루프를 멈춘다.

| 조건 | 전이 |
|---|---|
| 전체 테스트가 통과한 경우 | `PASSED` 또는 `DEPLOY_READY` |
| 최대 Retry에 도달한 경우 | `BLOCKED` |
| 동일한 오류 fingerprint가 2회 반복된 경우 | `BLOCKED` |
| 개인정보나 콘텐츠 확인이 필요한 경우 | `HITL_REQUIRED` |
| GitHub 인증 또는 배포 권한 문제가 발생한 경우 | `GITHUB_PERMISSION` 또는 `DEPLOYMENT` 후 `BLOCKED` |

## 7. Human-in-the-loop

다음 항목은 사람 확인이 필요하다.

- 이름, 소개, 경력, 프로젝트 등 개인 콘텐츠가 불명확한 경우
- 기존 콘텐츠 삭제가 필요한 경우
- 외부 분석 도구나 외부 서비스를 추가해야 하는 경우
- GitHub 저장소 설정을 변경해야 하는 경우
- 요구사항이 충돌하는 경우
- 개인 콘텐츠나 게임 난이도 설명이 불명확한 경우

## 8. 개발 루프 설계

### 루프별 공통 운영 원칙

- 한 루프는 하나의 기능 축만 다룬다.
- 실패 원인이 1개면 1개만 고친다.
- 파일 수정 후 검증을 반드시 다시 수행한다.
- 지배적인 실패 이유를 분류하고 다음 상태를 결정한다.

### 루프 표

| 단계 | 입력 | Act | Observe | 출력 | 테스트 기준 | 다음 상태 |
|---|---|---|---|---|---|---|
| 저장소 및 기존 파일 확인 | 빈 저장소, `README.md`, Git 상태 | 현재 구조와 누락 파일을 파악한다 | 루트 파일 존재 여부, 브랜치 상태, Pages 준비 여부 | 기준선 문서화 | 저장소가 정적 사이트 시작점인지 명확함 | `READY` |
| 정적 사이트 기본 구조 | 사이트 목적, GitHub Pages 제약 | `index.html` 중심 정보 구조를 설계한다 | 문서 뼈대와 섹션 흐름 확인 | Home/Games/푸터 골격 | 서버 없이 열리는 구조 | `ACTING` |
| 프로페셔널 콘텐츠 영역 | 개인 정보, 소개, 프로젝트 [사람 확인 필요] | 소개를 게임 중심 설명으로 대체한다 | 빈 콘텐츠와 실제 콘텐츠의 경계 확인 | 홈 섹션 카피 | 콘텐츠가 목적과 일치 | `HITL_REQUIRED` 또는 `PASSED` |
| 반응형 내비게이션 | 모바일 우선 요구 | 상단 탭과 메뉴 동작을 설계한다 | 모바일에서 접힘/겹침 여부 확인 | 탭형 내비게이션 | 360px에서 가로 스크롤 없음 | `VERIFYING` |
| Games 탭 | 게임 진입 요구 | Games 섹션 또는 페이지 연결을 설계한다 | 클릭/탭 진입 흐름 확인 | 게임 진입점 | Games 진입이 1단계로 가능 | `VERIFYING` |
| 곱셈 게임 핵심 로직 | 문제 생성, 자리수 옵션, 점수 규칙 | 랜덤 곱셈 문제와 정답 판정을 정의한다 | 랜덤성, 자리수 난이도, 정답/오답 판정 확인 | 곱셈 게임 규칙 또는 로직 설계 | 규칙이 단순하고 재현 가능 | `ACTING` |
| 키보드 조작 | 데스크톱 입력 | 숫자 입력, 백스페이스, 엔터 체계를 정의한다 | 입력 반응과 상태 전환 확인 | 키보드 입력 규칙 | 조작 지연 없음 | `VERIFYING` |
| 모바일 터치 조작 | 모바일 입력 | 화면 탭 시작과 클릭형 키패드 조작을 정의한다 | 터치 반응과 오입력 여부 확인 | 모바일 조작 규칙 | 한 손 조작 가능 | `VERIFYING` |
| 게임 UI 및 점수 | 점수판, 상태 메시지, 재시작 | UI 상태와 점수 표시를 정의한다 | 점수, 정답/오답, 재시작 확인 | 완성된 게임 UI | 상태가 사용자에게 명확히 보임 | `VERIFYING` |
| 접근성과 반응형 검증 | 데스크톱, 모바일, 접근성 기준 | 대비, 포커스, 버튼 크기, 읽기 순서를 점검한다 | 브라우저와 화면 크기별 표시 확인 | 접근성 수정 목록 | 주요 기능이 모든 화면에서 읽힘 | `VERIFYING` |
| GitHub Pages 호환성 검증 | 정적 사이트 자산 | 상대 경로와 정적 배포 가능성을 확인한다 | 빌드 도구 의존성, 서버 의존성, 경로 오류 확인 | Pages 호환성 결론 | 루트에서 정적으로 제공 가능 | `DEPLOY_READY` |
| 배포 | 승인된 저장소와 Pages 설정 [사람 확인 필요] | GitHub Pages 설정 및 배포 절차를 진행한다 | 배포 URL, 반영 여부 확인 | 공개 사이트 | 배포 후 실제 접근 가능 | `DEPLOYING` 후 `DEPLOYED` |

## 9. 상태 전이 규칙

```text
READY -> ACTING -> VERIFYING
VERIFYING -> PASSED
VERIFYING -> RETRYING
RETRYING -> ACTING -> VERIFYING
PASSED -> 다음 루프로 READY
모든 핵심 루프 PASSED -> DEPLOY_READY
DEPLOY_READY -> DEPLOYING -> DEPLOYED
어느 단계든 권한/콘텐츠 문제 -> HITL_REQUIRED
어느 단계든 환경/권한이 회복 불가 -> BLOCKED
```

## 10. 첫 번째 추천 루프

가장 안전한 첫 루프는 `저장소 및 기존 파일 확인`이다.

이유:
- 변경이 없다
- 사람 확인이 거의 필요 없다
- 이후 모든 루프의 기준선이 된다
- 정적 사이트와 게임 설계의 충돌을 초기에 발견할 수 있다

## 11. 현재 결론

- 저장소에는 아직 구현 파일이 없다
- `Games` 탭과 곱셈 게임은 새로 설계해야 한다
- 개인 콘텐츠는 현재 자료만으로는 확정할 수 없어 `HITL_REQUIRED`가 필요하다
- `Step 1`에서 별도의 `[게임 추가 기능:]` 항목은 확인되지 않았다

## 12. Self-Correcting TDD Loop

이 프로젝트는 GitHub Pages에서 동작하는 정적 HTML, CSS, JavaScript 웹사이트다.  
검증 가능한 항목만 사용하고, 현재 환경에서 실제로 실행 가능한 도구를 먼저 확인한 뒤 루프를 수행한다.  
이 단계에서는 아직 코드를 수정하거나 테스트를 실행하지 않는다.

### 12.1 기본 파일 검증

- 루트의 `index.html` 존재
- CSS 및 JavaScript 연결
- 잘못된 로컬 파일 경로 확인
- 대소문자 불일치 확인
- GitHub Pages에서 사용할 수 없는 절대 로컬 경로 확인

### 12.2 HTML 검증

- 기본 문서 구조
- `title`, `meta viewport`
- 시맨틱 태그
- 내비게이션 링크
- `Games` 영역
- 이미지 `alt` 속성
- 깨진 내부 링크

### 12.3 CSS 검증

- 데스크톱 화면
- 태블릿 화면
- 모바일 화면
- 가로 스크롤 발생 여부
- 내비게이션 및 `Games` UI 반응형 동작

### 12.4 JavaScript 검증

- 문법 오류
- 브라우저 콘솔 오류
- DOM 요소 `null` 참조
- 중복 이벤트 리스너
- 페이지 로드 시 오류

### 12.5 곱셈 게임 검증

- 게임 시작
- 다시 시작
- 랜덤 문제 생성
- 자리수 옵션에 따른 난이도 조절
- 점수 증가
- 정답/오답 판정
- 클릭형 키패드 입력
- 모바일 터치 조작

## 13. 현재 문제점

- 현재 확인된 구조적 문제점은 없다.
- `CHANGE_REQUEST.md`가 현재 작업 디렉터리에 없어 새 Self-Correcting TDD 재루프의 완료 기준을 확정할 수 없다. [사람 확인 필요]
- 브라우저에서 전개식 정렬, 셀별 오류 하이라이트, 힌트 메뉴 토글만 최종 확인하면 된다. [사람 확인 필요]

### 12.6 로컬 실행 검증

- 가능한 경우 `python3 -m http.server` 등의 로컬 정적 서버 사용
- HTTP 응답 확인
- `index.html` 정상 로드 확인
- CSS와 JavaScript 파일 응답 확인

### 12.7 브라우저 검증

가능한 도구가 있다면 다음 viewport를 확인한다.

- 모바일: 약 `375px`
- 태블릿: 약 `768px`
- 데스크톱: 약 `1440px`

### 12.8 GitHub Pages 호환성

- 루트 `index.html`
- 정적 상대 경로
- 서버 전용 기능 미사용
- 로컬 파일 시스템 의존성 미사용
- 백엔드 API 의존성 미사용

## 14. 개인 정보 반영

- `resume.txt` 파일을 먼저 읽고, 그 내용을 기준으로 `About`, 소개 문구, 프로필, 프로젝트 설명에 나에 대한 정보를 반영한다.
- `resume.txt`에 없는 정보는 임의로 추측하지 말고 `[사람 확인 필요]`로 남긴다.

### 12.9 실패 로그 수집

실패 로그에서는 다음을 수집한다.

- 실행 명령어
- exit code
- 실패한 검증 항목
- 핵심 오류 메시지
- 관련 파일과 라인
- 브라우저 콘솔 메시지
- 오류 fingerprint

### 12.10 실패 원인 분류

실패 원인은 다음 중 하나로만 분류한다.

- `HTML_STRUCTURE`
- `CSS_RESPONSIVE`
- `JAVASCRIPT`
- `GAME_LOGIC`
- `GAME_CONTROL`
- `CONTENT`
- `TEST`
- `ENVIRONMENT`
- `GITHUB_PERMISSION`
- `DEPLOYMENT`
- `UNKNOWN`

### 12.11 최소 수정 원칙

- 한 Retry에서는 하나의 원인만 수정
- 관련 파일만 수정
- 테스트 삭제 또는 검증 기준 완화 금지
- 전체 사이트의 불필요한 재작성 금지
- 이미 통과한 기능을 깨뜨리는 수정 금지
- 외부 프레임워크로 임의 전환 금지

### 12.12 Retry 정책

- 하나의 오류에 대해 최대 3회
- 동일 오류 fingerprint가 2회 반복되면 중지
- 각 Retry마다 가설, 변경 파일, 실행 명령어, 결과 기록
- 환경 또는 권한 문제는 코드 수정으로 해결하려 하지 않음

### 12.13 Verifier 사용

가능하면 Claude Code CLI를 독립 Verifier로 활용한다.  
Claude Code CLI에서 Sonnet 5 사용 가능 여부를 먼저 확인하고, 실제로 사용 가능한 경우에만 Sonnet 5를 사용한다.  
사용할 수 없다면 현재 사용 가능한 Sonnet 모델을 사용하고 실제 모델명을 기록한다.  

### 12.14 상태 운영

이 단계의 상태는 다음 중 하나를 사용한다.

- `READY`
- `ACTING`
- `VERIFYING`
- `RETRYING`
- `PASSED`
- `DEPLOY_READY`
- `DEPLOYING`
- `DEPLOYED`
- `BLOCKED`
- `HITL_REQUIRED`

### 12.15 루프별 적용 순서

1. 저장소 및 기존 파일 확인
2. 정적 사이트 기본 구조
3. 프로페셔널 콘텐츠 영역
4. 반응형 내비게이션
5. `Games` 탭
6. 곱셈 게임 핵심 로직
7. 키보드 조작
8. 모바일 터치 조작
9. 게임 UI 및 점수
10. 접근성과 반응형 검증
11. GitHub Pages 호환성 검증
12. 배포

### 12.16 첫 루프 판단

가장 안전한 첫 번째 루프는 `저장소 및 기존 파일 확인`이다.  
그 다음은 정적 사이트 기본 구조와 프로페셔널 콘텐츠 영역을 분리해서 진행한다.
